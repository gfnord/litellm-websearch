# Core litellm with proxy extras
litellm[proxy]>=1.83.14
# fastapi 0.140.7 removed fastapi.dependencies.utils.get_flat_dependant, which
# litellm <=1.97.0 imports; litellm's own pin (>=0.136.3,<1.0) allows the broken versions.
fastapi<0.140.7
